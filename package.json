{
  "name": "lora-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Lora - Personal AI Mobile App Builder",
  "scripts": {
    "dev": "concurrently -k -n bridge,expo -c blue,magenta \"npm run bridge\" \"npm run expo\"",
    "bridge": "cd bridge-server && npm run build && npm start",
    "expo": "cd mobile && npx expo start --tunnel --clear",
    "bridge:dev": "cd bridge-server && npm run dev",
    "expo:local": "cd mobile && npx expo start",
    "install:all": "npm install && cd bridge-server && npm install && cd ../mobile && npm install",
    "build:bridge": "cd bridge-server && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
